import { createFromIconfontCN } from "@ant-design/icons";
import iconFontUrl from "@/assets/font/iconfont.js";

const Icon = createFromIconfontCN({
  scriptUrl: iconFontUrl, // 在 iconfont.cn 上生成
});

export default Icon;
